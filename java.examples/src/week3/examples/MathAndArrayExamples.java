package week3.examples;

import java.util.Scanner;

public class MathAndArrayExamples {
    public static void main(String[] args) {
//   System.out.println(Math.PI);
//   System.out.println(Math.round(6.74));
// 
//   System.out.println(Math.sqrt(16));
//   System.out.println(Math.pow(2,5));

        // System.out.println((int)(Math.random()*90)+10);

        // Question 1:
        /*
         * int[] a= {4,6,1,8,9,7};
         * System.out.println("a[3]="+a[3]+" a[5]="+a[5]);
         * 
         * System.out.println("length:"+a.length);
         * for(int i=0;i<a.length;i++) {
         * System.out.print(a[i]+" ");
         * }
         * 
         * System.out.println();
         */
        // Question 2: Write a program which takes the size and the values of
        // the array from the user, and finds the number of elements
        // which are divisible by 5. Put these values into a new array
//        Scanner scanner = new Scanner(System.in);
//        System.out.println("Enter Length:");
//        int length = scanner.nextInt();
//        int[] userNumbers = new int[length];
//        int counter = 0;
//        for (int i = 0; i < userNumbers.length; i++) {
//            System.out.println("userNumbers[ " + i + "]=?");
//            userNumbers[i] = scanner.nextInt();
//            if (userNumbers[i] % 5 == 0) {
//                counter++;
//            }
//        }
//        int[] userNumbers2 = new int[counter];
//        int j = 0;
//        for (int i = 0; i < userNumbers.length; i++) {
//            if (userNumbers[i] % 5 == 0) {
//                userNumbers2[j] = userNumbers[i];
//                j++;
//            }
//        }
//
//        for (int value : userNumbers2) {
//            System.out.print(value + " ");
//        }
//
//        System.out.println();
        
        
        
        
        // Question10: Create an array with the size of 100 and fill it
        // with random integers between [10,99]. numbers
        // create another array with numbers<50
        /*
         * int[] numbers=new int[100];
         * int count=0;
         * for(int i=0;i<numbers.length;i++) {
         * numbers[i] = (int)(Math.random()*90)+10;
         * if(numbers[i]<50) {
         * count++;
         * }
         * }
         * System.out.println(numbers);
         * System.out.println(count);
         * 
         * int[] secondArray=new int[count];
         * int j=0;
         * for(int i=0;i<numbers.length;i++) {
         * if(numbers[i]<50) {
         * secondArray[j] = numbers[i];
         * System.out.print(secondArray[j]+" ");
         * j++;
         * }
         * }
         * 
         */

        
    }
}
